#!/bin/bash

# Script to ensure that ~/.profile and ~/.bashrc have been source.
#It is called at the end of ~/.bashrc

if [ ! -f "${HOME}/.sourcedbashrc" ]; then
  source ~/.profile
elif [ -f "${HOME}/.sourcedbashrc" ]; then
  /bin/rm ~/.sourcedbashrc
fi
